# dependencies
/node_modules

# next.js
/.next/
/out/

# NOTE: public/data/ is intentionally COMMITTED so the deployed site (e.g. Vercel)
# ships with the data. scripts/prepare-data.mjs refreshes it locally from the
# pipeline outputs when present, and exits without touching it when absent.

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log
